local finish = MoveEvent()

function finish.onStepIn(player, item, position, fromPosition)
	if player:isPlayer() then
		player:teleportTo(Position(10504, 10490, 7))
	end
	return true
end

finish:type("stepin")
finish:aid(6436)
finish:register()


local finish = MoveEvent()

function finish.onStepIn(player, item, position, fromPosition)
	if player:isPlayer() then
		player:teleportTo(Position(10412, 10422, 7))
	end
	return true
end

finish:type("stepin")
finish:aid(6437)
finish:register()


local finish = MoveEvent()

function finish.onStepIn(player, item, position, fromPosition)
	if player:isPlayer() then
		player:teleportTo(Position(10436, 10452, 7))
	end
	return true
end

finish:type("stepin")
finish:aid(6438)
finish:register()


local finish = MoveEvent()

function finish.onStepIn(player, item, position, fromPosition)
	if player:isPlayer() then
		player:teleportTo(Position(10520, 10434, 7))
	end
	return true
end

finish:type("stepin")
finish:aid(6439)
finish:register()
