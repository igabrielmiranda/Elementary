local finish = MoveEvent()

function finish.onStepIn(player, item, position, fromPosition)
	if player:isPlayer() then
		player:teleportTo(Position(3567, 3494, 8))
	end
	return true
end

finish:type("stepin")
finish:aid(25481)
finish:register()


local finish = MoveEvent()

function finish.onStepIn(player, item, position, fromPosition)
	if player:isPlayer() then
		player:teleportTo(Position(3424, 3392, 10))
	end
	return true
end

finish:type("stepin")
finish:aid(25482)
finish:register()


local finish = MoveEvent()

function finish.onStepIn(player, item, position, fromPosition)
	if player:isPlayer() then
		player:teleportTo(Position(3591, 3430, 12))
	end
	return true
end

finish:type("stepin")
finish:aid(25483)
finish:register()


local finish = MoveEvent()

function finish.onStepIn(player, item, position, fromPosition)
	if player:isPlayer() then
		player:teleportTo(Position(3577, 3440, 11))
	end
	return true
end

finish:type("stepin")
finish:aid(25484)
finish:register()
